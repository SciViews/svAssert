% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stopifnot_.R
\name{stopifnot_}
\alias{stopifnot_}
\alias{get_stop_fun}
\alias{mod_not}
\alias{mod_content}
\title{Assert that expressions are all TRUE, with extended error messages}
\usage{
stopifnot_(...)

get_stop_fun(
  x = NULL,
  expr = substitute(x),
  par. = list(),
  call_it = TRUE,
  force_stop = TRUE
)

mod_not(mod)

mod_content(x, expr)
}
\arguments{
\item{...}{Any number of R expressions, which should each evaluate to (a
logical vector of all) \link{TRUE}.}

\item{x}{An expression to analyze}

\item{expr}{An expression object}

\item{par.}{A list of additional parameters to pass to the stop function,
optionally containing  \code{msg} a string with custom message(s), \code{arg} a single
string with the name of the argument that failed the assertion, \code{mod} a
single string with starting modifier (it is recomputed), \code{id} an
identifier to append to the error class, or \code{call} the call where the error
was generated. Default is an empty list.}

\item{call_it}{If \code{TRUE}, the corresponding \code{stop_xxx()} function is called
(if it exists) to generate the error message. If \code{FALSE}, see \code{force_stop}.
Default is \code{TRUE}.}

\item{force_stop}{If \code{call_it} is \code{TRUE} and no \code{stop_xxx()} function exists
for the core expression, or if it exists but does not stop, and if
\code{force_stop = TRUE} (default), a generic error message is generated and the
function stops. If \code{force_stop = FALSE}, no error is raised and information
about the stop function is returned.}

\item{mod}{A modifier string (could be \code{""}, \code{"!"}, \code{"any"}, \code{"all"},
\code{"!any"}, \code{"!all"}, or any combination of these).}
}
\value{
For \code{stopifnot_()}, \link{NULL} if all statements in ... are TRUE.

For \code{get_stop_fun(call_it = FALSE)}, a list with \code{stop_fun} the name
of the stop function, \code{call} the call to the stop function (if it exists,
otherwise \code{NULL}), \code{mod} the modifier, and \code{expr} the core expression,
excluding the modifier.
\code{mod} can be \code{NULL} or \code{""} if no modifier is present. Otherwise, it is "!"
(not true), "any" (at least one element is true), "all" (all elements are
true), "!any" (all elements are wrong), or "!all" (at least one element is
wrong). The error message build by your \code{stop_xxx()} functions should take
this modifier into account to issue the correct error message for the core
expression.

For \code{mod_not()}, \code{TRUE} if the modifier starts with "!", \code{FALSE}
otherwise.

For \code{mod_content()}, a message with the content of \code{expr} and its
value \code{x}.
}
\description{
If any of the expressions (in ...) are not \link{all} TRUE, an error is raised. If
a \code{stop_expr()} function exists for the expression \code{expr}, it is called to
generate the error message, otherwise a default message is created.
}
\details{
\code{get_stop_fun()} and \code{mod_not()} are utility functions to compute the stop
function call and manage a modifier (\link{!}, \code{\link[=any]{any()}} or \code{\link[=all]{all()}}) in expressions
to ease building error messages.
}
\examples{
# stop <- stop_
# Note that |> try() is just there to catch error; do not use in your code!
x <- 1
stopifnot_(1 == 1, all.equal(pi, 3.14159265), x < 2) # No error
stopifnot_(1 == 1, all.equal(pi, 3.14159265), x > 2) |> try()
# Compare with the message you got using base::stopifnot():
stopifnot(1 == 1, all.equal(pi, 3.14159265), x > 2) |> try()

stopifnot_(is.character(letters), length(letters) == 1) |> try()
stopifnot_(all.equal(pi, 3.141593), 2 < 2, (1:10 < 12), "a" < "b") |> try()

# get_stop_fun() returns infos about a stop function when call_it = FALSE
get_stop_fun(length(letters) == 1L, call_it = FALSE)
get_stop_fun(length("a") != 1L, call_it = FALSE) # mod == "!", expr = "=="
get_stop_fun(!any(c(TRUE, TRUE, NA)), call_it = FALSE) # mod == "!any"
# all! is the same as !any (and any! is !all)
get_stop_fun(all(!c(TRUE, FALSE, NA)), call_it = FALSE) # mod == "!any"
# Contrived and weird example: it got simplified for mod
get_stop_fun(all(any(!!all(!anyNA(x)))), call_it = FALSE) # mod == "!any"
# Call the stop function (if it exists) to raise the error
get_stop_fun(is.numeric(letters) && length(letters) > 0L) |> try()

# mod_not() can be used exclusively to build either a positive, or a negative
# sentence in your error message when the expression always returns a single
# logical value, because in this case "any" or "all" have no effect.
stop_length_one <- function(x, ..., par. = list()) {
  arg <- lbl(.op$arg \%||\% par.$arg \%||\% substitute(x))

  # length(x) == 1 always returns a single logical, can use mod_not() here
  # and safely ignore 'any' or 'all' modifiers
  if (mod_not(par.$mod)) {
    stop(
      "!" = "{.code {arg}} cannot have length 1.")
  } else {
    stop(
      "!" = "{.code {arg}} must have length 1",
      "*" = "Its length is {length(x)}.")
  }
}
length("a") == 1 || stop_length_one("a")
(length(letters) == 1 || stop_length_one(letters)) |> try()
# This avoids writing two stop_xxx() functions, one for == and one for !=
(length("a") != 1 || stop_length_one("a", par. = list(mod = "!"))) |> try()
(!length("a") == 1 || stop_length_one("a", par. = list(mod = "!"))) |> try()
# any() or all() have no effect on single logical and can then be ignored
(any(length("a") != 1) ||
  stop_length_one("a", par. = list(mod = "!all"))) |> try()
rm(stop)
}
\seealso{
\code{\link[=stop_]{stop_()}}, \code{\link[base:stop]{base::stop()}}
}
