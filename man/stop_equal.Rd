% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stop_equals.R
\name{stop_equal}
\alias{stop_equal}
\alias{stop_==}
\title{Stop function for equality check}
\usage{
stop_equal(
  lhs,
  rhs,
  ...,
  mod = NULL,
  na.rm = FALSE,
  test_it = TRUE,
  par. = list()
)

`stop_==`(
  lhs,
  rhs,
  ...,
  mod = NULL,
  na.rm = FALSE,
  test_it = TRUE,
  par. = list()
)
}
\arguments{
\item{lhs}{The left-hand side of the comparison.}

\item{rhs}{The right-hand side of the comparison.}

\item{...}{Additional arguments (not used yet).}

\item{mod}{A character string indicating the modifier to use for the
comparison (\code{""}, \code{"any"}, or \code{"all"}). Default is \code{NULL}, which is
equivalent to \code{""}.}

\item{na.rm}{Logical, indicating whether missing values should be removed
before performing the comparison. Default is \code{FALSE}.}

\item{test_it}{Logical, indicating whether the comparison should be actually
tested. Default is \code{TRUE}. If \code{FALSE}, the function only constructs the
error message based on the lengths of \code{lhs} and \code{rhs}, and the presence of
missing values.}

\item{par.}{A list of parameters to customize the error message. Optional
fields include \code{msg} (message(s) to display at the top), \code{footer}(message(s)
to add at the bottom), \code{arg} (name of first argument), \code{arg2} (name of second
argument), \code{id} (error class id), \code{mod} (the modifier, see \code{mod} above),
\code{call} (the call where the error was generated).}
}
\value{
This function always stops with an error.
}
\description{
\code{stop_equal()} generates an error message for \code{==} assertions.
}
\examples{
stop_equal(1, 2) |> try() # A little bit silly with two constants
x <- 1
stop_equal(x, 2) |> try()
x <- 1:2
stop_equal(x, 1) |> try() # Not length 1
stop_equal(x, 1:2, mod = "all") |> try() # Should not call stop_equal() here
stop_equal(x, 1:3, mod = "all") |> try()
stop_equal(x, 1:3, mod = "any") |> try() # Should not call stop_equal() here
stop_equal(x, 2:4, mod = "any")
y <- 2
stop_equal(x, y, mod = "all") |> try()
stop_equal(x, y, mod = "any") |> try() # Should not call stop_equal() here
z <- c(NA, 5, NA)
stop_equal(x, z, mod = "all") |> try()
stop_equal(x, z, mod = "all", na.rm = TRUE) |> try()
stop_equal(x, z, mod = "any", na.rm = TRUE) |> try()
stop_equal(length(x), 1L) |> try()
stop_equal(NULL, 2L) |> try()
# Extra messages
stop_equal(y, 2, par. = list(msg = c("Some info...", x = "This is wrong"),
  footer = c("*" = "A footer..."))) |> try()
}
