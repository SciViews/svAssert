% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stop_funs.R
\name{stop_equals}
\alias{stop_equals}
\alias{stop_==}
\title{Stop function for equality or inequality check}
\usage{
stop_equals(lhs, rhs, ..., par. = list())

`stop_==`(lhs, rhs, ..., par. = list())
}
\arguments{
\item{lhs}{The left-hand side of the comparison.}

\item{rhs}{The right-hand side of the comparison.}

\item{...}{Additional arguments (not use)d yet).}

\item{par.}{A list of parameters to customize the error message. Optional
fields include \code{msg} (message(s) to display at the top), \code{footer}(message(s)
to add at the bottom), \code{arg} (name of first argument), \code{arg2} (name of second
argument), \code{id} (error class id), \code{mod} (the modifier, that is "" for \code{==}
and "!" for \code{!=}) \code{call} (the call where the error was generated).}
}
\value{
This function always stops with an error.
}
\description{
\code{stop_equals()} generates an error message for both \code{==} and \code{!=} assertions.
}
\examples{
stop_equals(1, 2) |> try()
x <- 1
stop_equals(x, 2) |> try()
stop_equals(x, 1, par. = list(mod = "!")) |> try()
y <- 2
stop_equals(x, y) |> try()
z <- 1
stop_equals(x, z, par. = list(mod = "!")) |> try()
stop_equals(length(x), 2L) |> try()
# Extra message
stop_equals(2, x, par. = list(msg = c("Some info...", x = "This is wrong"),
  footer = c("*" = "A footer..."))) |> try()
}
